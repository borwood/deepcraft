# 0080 — the window that sees the frame

STUB — expanded before the commit that lands the slice. Placeholder so the
worktree is not auto-cleaned.

The perf-observability slice: the first thing that lets us see where per-frame
time goes. To be written as narrative — the observability gap, the S-3
authority/consumer shape (the aggregate is the authority; the docs dump and the
future in-game overlay are both consumers), the overlay heir, what the spans
wrap and why those are the suspects, the zero-cost-when-off design, and (if
captured) the first ranked killer list.
