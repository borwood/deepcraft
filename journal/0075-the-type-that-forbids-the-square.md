# 0075 — the type that forbids the square

*2026-07-22. The by-construction end-state of the threshold-quantization
migration (spines § S-4 "Ratified end-state"): `CoarseField<T>` and its two
traits, extracted in dc-core from what the two shape-teachers paid for
(journal/0072 move A, journal/0073 move B). STUB — measurements and the full
narrative land with the gate-green commit.*

> blogworthy (lens: AI-native × deepsim-reflexions): **a type extracted from
> the lessons its teachers bled for.** The migration did not design the boundary
> type first and hunt for callers — it converted the two cheapest hot seams,
> let each teach one move, and only then froze the vocabulary. Every method on
> `CoarseField<T>` has a scar behind it: the anchored blend exists because A1's
> naïve dot broke the byte-identity off-switch sub-ULP; `summarize` exists
> because B1's white noise doubled the far mesh; the boundary membership dither
> exists because the user watched minority phases guillotine at cell perimeters
> and called it the cake-slice. The type is the fossil record of its own
> derivation.

## What is being built (fills in as it lands)

- `dc-core/src/coarse.rs`: `CoarseField<T>`, `Interpolable` (move A), `ShareVec`
  (the quantity both moves read), the inverse-CDF draw + `summarize` + the
  cake-law boundary membership dither. Generic law-tests including the
  compile-fail proof that the raw per-cell read is inexpressible.
- A1 migrated behind the type, byte-identical (goldens must NOT move).
- The pinned pair consolidated: `outcrop_at = argmax ∘ outcrop_shares`, one slot.

(The rest of this entry is written against the measured result.)
