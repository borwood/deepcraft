# 0073 — the frontier that became a gradient

*STUB — shape-teacher #2 (audit B1): `surface_class` plurality → membership
dither. In progress.*

> blogworthy: lens 3 (deepsim reflexions) + lens 4 (respect for earth). A soil
> frontier in nature IS a gradient, not a line — and the move-B API lesson for
> `CoarseField<T>`.
