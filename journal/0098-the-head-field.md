# 0098 — the head field (WIP skeleton)

FLOW continuation (a): the potential/head field pass, and its first consumer —
the vertical (slot<->slot) faces that slice 1 left honestly zero.

*(in progress)*
