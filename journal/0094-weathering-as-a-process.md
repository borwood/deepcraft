# 0094 — Weathering as a PROCESS, not a snapshot

*(skeleton — in progress; Movement 3 of the weathering arc, discharges stub #17)*

S18 (journal/0089) built the inventory-weathering machinery but ran it **once,
post-hoc**, over the finished record with frozen fields. Sub-voxel (0.04 m),
invisible (corrections #46/#47, stub #17). This entry relocates weathering into
the deep-time loop as a **declared per-epoch cellular pass** that **accumulates**,
so the saprolite band grows to ≥1 voxel.

Narrative to follow: the snapshot→process move, the span-index crux (stable
bedrock keying against a growing record), the `dt`-goes-live step, the
two-authorities split (reads H, writes only the ledger), the production-scale
band number.
