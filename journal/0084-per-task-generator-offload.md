# 0084 — the per-task generator: neighbour fill and far derive leave the frame thread

*(STUB — in progress. Follow-on to journal/0083. Pushing `neighbor_fill.gen` and
`far_tile.derive` off the frame thread via a per-task `WorldGenerator` minted
from the shared `Arc<Pregen>`.)*
