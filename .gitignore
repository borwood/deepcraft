/target
# The demo plugin builds into its own target dir (it is excluded from the
# workspace; see plugins/demo-builder/Cargo.toml).
/plugins/demo-builder/target
**/*.rs.bk
*.pdb
# Background-agent worktrees (created/removed by the session workflow; a
# stale one here once got committed as a gitlink — see journal/0006 era).
/.claude/worktrees
